import { StyleBrothersDirective } from './style-brothers.directive';

describe('StyleBrothersDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleBrothersDirective();
    expect(directive).toBeTruthy();
  });
});
